// ServerTable — server-side paginated table
// Built on @tanstack/react-table with external pagination state
// Implement in Phase 2

const ServerTable = () => null;
export { ServerTable };
export default ServerTable;
